
#include "Partitioner.hpp"
#include "../../../utils/vecutil.hpp"

std::vector<DataManager *> Partitioner::partition(const hi::HIBuildParams& params, DataManager *pdata, int depth) {
//    const size_t R = pdata->getRows();
//    const size_t C = pdata->getCols();
//    auto pivots = vecutil::randRowPointers<Dat>(R, C, params.maxPivots, pdata->getDat());
//    for (auto pivot : pivots){
//        pdata->sort(pivot);
//
//    }
//    return std::vector<DataManager *>();
}
